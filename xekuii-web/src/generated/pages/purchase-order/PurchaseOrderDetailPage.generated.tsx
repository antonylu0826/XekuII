// Auto-generated by XekuII.Generator - DO NOT EDIT
// Generated at: 2026-02-06 17:40:23

import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { useNavigate, useParams } from "@tanstack/react-router";
import { useState } from "react";
import { purchaseOrderApi } from "../../api/purchase-order.api.generated";
import type { PurchaseOrderItemSummary } from "../../types/purchase-order.types.generated";
import { PurchaseStatusLabels } from "../../types/purchase-order.types.generated";
import { PageLoading } from "@/components/shared/LoadingSpinner";
import { ConfirmDialog } from "@/components/shared/ConfirmDialog";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Pencil, Trash2, ArrowLeft } from "lucide-react";
import { toast } from "sonner";

export function PurchaseOrderDetailPage() {
  const navigate = useNavigate();
  const queryClient = useQueryClient();
  const { id } = useParams({ strict: false }) as { id: string };
  const [showDelete, setShowDelete] = useState(false);

  const { data, isLoading } = useQuery({
    queryKey: ["purchaseOrder", id, "details"],
    queryFn: () => purchaseOrderApi.getDetails(id),
  });

  const deleteMutation = useMutation({
    mutationFn: () => purchaseOrderApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["purchaseOrders"] });
      toast.success("採購單 deleted");
      navigate({ to: "/purchase-orders" });
    },
    onError: () => toast.error("Failed to delete 採購單"),
  });

  if (isLoading || !data) return <PageLoading />;

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const item = data as any;

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-3">
          <Button variant="ghost" size="icon" onClick={() => navigate({ to: "/purchase-orders" })}>
            <ArrowLeft className="h-4 w-4" />
          </Button>
          <h1 className="text-2xl font-bold">採購單 Detail</h1>
        </div>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => navigate({ to: "/purchase-orders/$id/edit", params: { id } })}>
            <Pencil className="mr-2 h-4 w-4" /> Edit
          </Button>
          <Button variant="destructive" onClick={() => setShowDelete(true)}>
            <Trash2 className="mr-2 h-4 w-4" /> Delete
          </Button>
        </div>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>基本資訊</CardTitle>
        </CardHeader>
        <CardContent>
          <dl className="grid grid-cols-2 gap-4">
            <div>
              <dt className="text-sm font-medium text-muted-foreground">採購單號</dt>
              <dd className="mt-1">{String(item.poNumber ?? "-")}</dd>
            </div>
            <div>
              <dt className="text-sm font-medium text-muted-foreground">採購日期</dt>
              <dd className="mt-1">{String(item.orderDate ?? "-")}</dd>
            </div>
            <div>
              <dt className="text-sm font-medium text-muted-foreground">供應商</dt>
              <dd className="mt-1">{String((item.supplier as Record<string, unknown>)?.name ?? item.supplier ?? "-")}</dd>
            </div>
            <div>
              <dt className="text-sm font-medium text-muted-foreground">狀態</dt>
              <dd className="mt-1"><Badge variant="outline">{(PurchaseStatusLabels as Record<number, string>)[item.status as number] ?? String(item.status)}</Badge></dd>
            </div>
          </dl>
        </CardContent>
      </Card>
      <Card>
        <CardHeader>
          <CardTitle>金額統計</CardTitle>
        </CardHeader>
        <CardContent>
          <dl className="grid grid-cols-2 gap-4">
            <div>
              <dt className="text-sm font-medium text-muted-foreground">總金額</dt>
              <dd className="mt-1">{String(item.totalAmount ?? "-")}</dd>
            </div>
          </dl>
        </CardContent>
      </Card>
      <Card>
        <CardHeader>
          <CardTitle>採購明細</CardTitle>
        </CardHeader>
        <CardContent>
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>數量</TableHead>
                <TableHead>進貨單價</TableHead>
                <TableHead>小計</TableHead>
                <TableHead>採購產品</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {(item.items as PurchaseOrderItemSummary[] | undefined)?.map((row) => (
                <TableRow key={row.id ?? (row as any).oid}>
                  <TableCell>{String(row.quantity ?? "-")}</TableCell>
                  <TableCell>{String(row.unitPrice ?? "-")}</TableCell>
                  <TableCell>{String(row.subtotal ?? "-")}</TableCell>
                  <TableCell>{String(row.productName ?? "-")}</TableCell>
                </TableRow>
              )) ?? (
                <TableRow><TableCell colSpan={4} className="text-center text-muted-foreground">No items</TableCell></TableRow>
              )}
            </TableBody>
          </Table>
        </CardContent>
      </Card>

      <ConfirmDialog
        open={showDelete}
        onOpenChange={setShowDelete}
        title="Delete 採購單"
        description="Are you sure you want to delete this 採購單? This action cannot be undone."
        onConfirm={() => deleteMutation.mutate()}
        loading={deleteMutation.isPending}
      />
    </div>
  );
}

