// Auto-generated by XekuII.Generator - DO NOT EDIT
// Generated at: 2026-02-06 17:40:23

import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { useNavigate, useParams } from "@tanstack/react-router";
import { supplierApi } from "../../api/supplier.api.generated";
import { createSupplierSchema } from "../../schemas/supplier.schema.generated";
import type { CreateSupplierDto } from "../../types/supplier.types.generated";
import { EntityForm, type FieldConfig, type FormRowConfig } from "@/components/shared/EntityForm";
import { PageLoading } from "@/components/shared/LoadingSpinner";
import { toast } from "sonner";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Eye, Pencil, Trash2, Plus, ArrowLeft } from "lucide-react";

const fieldConfigs: FieldConfig[] = [
  { name: "name", label: "供應商名稱", type: "text" as const },
  { name: "contactPerson", label: "聯絡人", type: "text" as const },
  { name: "phone", label: "聯絡電話", type: "text" as const },
  { name: "address", label: "地址", type: "text" as const },
];

const formLayout: FormRowConfig[] = [
  { fields: ["name"] },
  { fields: ["contactPerson", "phone"] },
  { fields: ["address"] },
];

export function SupplierFormPage() {
  const navigate = useNavigate();
  const queryClient = useQueryClient();
  const params = useParams({ strict: false });
  const id = (params as Record<string, string | undefined>).id;
  const isEdit = !!id;

  const { data: existing, isLoading } = useQuery({
    queryKey: ["supplier", id],
    queryFn: () => supplierApi.getById(id!),
    enabled: isEdit,
  });

  const createMutation = useMutation({
    mutationFn: (data: CreateSupplierDto) => supplierApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["suppliers"] });
      toast.success("供應商 created");
      navigate({ to: "/suppliers" });
      },
    onError: () => toast.error("Failed to create 供應商"),
  });

  const updateMutation = useMutation({
    mutationFn: (data: CreateSupplierDto) => supplierApi.update(id!, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["suppliers"] });
      queryClient.invalidateQueries({ queryKey: ["supplier", id] });
      toast.success("供應商 updated");
      navigate({ to: "/suppliers" });
      },
    onError: () => toast.error("Failed to update 供應商"),
  });

  if (isEdit && isLoading) return <PageLoading />;

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const ex = existing as any;
  const defaultValues = isEdit && ex
    ? {
      name: ex.name ?? "",
      contactPerson: ex.contactPerson ?? "",
      phone: ex.phone ?? "",
      address: ex.address ?? "",
    }
    : {
      name: "",
      contactPerson: "",
      phone: "",
      address: "",
    };

  return (
    <div className="max-w-2xl space-y-4">
      <h1 className="text-2xl font-bold">{isEdit ? "Edit" : "New"} 供應商</h1>
      <EntityForm
        schema={createSupplierSchema}
        defaultValues={defaultValues}
        fields={fieldConfigs}
        layout={formLayout}
        onSubmit={(data) => isEdit ? updateMutation.mutate(data as CreateSupplierDto) : createMutation.mutate(data as CreateSupplierDto)}
        loading={createMutation.isPending || updateMutation.isPending}
        submitLabel={isEdit ? "Update" : "Create"}
        onCancel={() => navigate({ to: "/suppliers" })}
      />
    </div>
  );
}

